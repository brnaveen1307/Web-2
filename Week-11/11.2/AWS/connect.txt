ssh -i nameofthefile(.pem) ubuntu@ipAddress

chmod 700 nameofthefile(.pem) 

file(key pair)

install node.js through nvm


nginx - reverse proxying
installing nginx: sudo apt update
                  sudo apt install nginx

create a proxy server

sudo vi /etc/nginx/nginx.conf

events{
    # Event directives...
}

http{
    server{
    listen: 80;
    server_name be1.100xdevs.com;

    location / {
        proxy_pass http://localhost:8080;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_cache_bypass $http_upgrade;
    }
    }
}

sudo nginx -s reload

//Certificate Management

certBot installation: sudo snap install --classic certBot
                      sudo ln -s /snap/bin/certbot /usr/bin/certbot
                      sudo certbot --nginx

Run the project in Bckground: pm2 start index.js